#include "PredatorsAnimals.h"

PredatorsAnimals::PredatorsAnimals(const char* typeFood, const double biteForce, const int& runningSpeed):Animals(typeFood),mBiteForce(biteForce),mRunningSpeed(runningSpeed)
{
}
